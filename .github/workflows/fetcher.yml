name: Fetch and Zip Files

on:
  schedule:
    - cron: '0 * * * *'  # Runs every hour

  workflow_dispatch:

jobs:
  fetch_and_zip:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Fetch and Zip
      run: |
        curl -X POST -H "Accept: application/json" -H "Authorization: LOW ${{ secrets.myaccesskey }}:${{ secrets.mysecret }}" -d'url=https://scratchbox.grady.link/api/projects&capture_outlinks=1&capture_all=1' https://web.archive.org/save
